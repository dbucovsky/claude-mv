# Home

Welcome to the `claude-mv` wiki.

`claude-mv` moves a project folder together with its Claude Code context (chat sessions, history, file-history, todos, shell snapshots, and debug logs), so relocating or renaming a project doesn't orphan its history.

## Pages

- [[Usage]] — full command reference, including the `.anchor` location-tracking workflow
